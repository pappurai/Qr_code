import * as React from 'react';
import { Text } from 'react-native';

// react-native-custom-qr-codes-expo does not work on Expo Web
export default function CustomQRCodes() {
  return (
    <Text>
      Sorry, react-native-custom-qr-codes-expo does not work on Expo Web
    </Text>
  );
}
